# Generated / tool-owned
node_modules/
allure-report/
allure-results/
playwright-report/
test-results/
.auth/

# Markdown test plans: the format must stay byte-compatible with what the
# Playwright planner agent's `planner_save_plan` tool emits. Prettier dedents
# the `- expect:` lines and inserts blank lines, which breaks that parity.
specs/

# Regenerated by `npm run agents:init` — never hand-formatted.
.claude/agents/
